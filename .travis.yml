sudo: false
language: java
jdk:
- oraclejdk8
node_js: "0.10"
install:
  - npm install -g snyk && snyk test
cache:
  directories:
  - $HOME/.gradle

env:
  - TERM=dumb
script: mvn clean test -Djenkins.version=2.7.3 -Denforcer.skip=true -B
after_success:
  - snyk monitor
